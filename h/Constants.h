namespace Constants {
 	const int SquareCount = 8;
	const int SquareSize = 80;
	constexpr const int BoardSize = SquareCount * SquareSize;
	
	const Color Light(232, 235, 239);
	const Color Dark(125, 135, 150);
	const Color Cyan(173, 216, 230);
	const Color Grey(169, 169, 169);
}